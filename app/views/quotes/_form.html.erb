<%= form_with(model: quote, local: true) do |form| %>
  <% if quote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(quote.errors.count, "error") %> prohibited this quote from being saved:</h2>

      <ul>
      <% quote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Type %>
    <%= form.text_field :qtype %>
  </div>

  <div class="field">
    <%= form.label :Model %>
    <%= form.text_field :qmodel %>
  </div>

  <div class="field">
    <%= form.label :Serial %>
    <%= form.text_field :qserial %>
  </div>

  <div class="field">
    <%= form.label :Symptoms %>
    <%= form.text_area :qsymptoms %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
