<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Type %>
    <%= form.text_field :dtype %>
  </div>

  <div class="field">
    <%= form.label :Model %>
    <%= form.text_field :dmodel %>
  </div>

  <div class="field">
    <%= form.label :Serial %>
    <%= form.text_field :dserial %>
  </div>

  <div class="field">
    <%= form.label :Symptoms %>
    <%= form.text_area :dsymptoms %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
